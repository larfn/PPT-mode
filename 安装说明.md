# 模板助手安装说明

适用于 Windows 10 / 11 桌面版 PowerPoint（Microsoft 365 或 Office 2016+）。

## 推荐：使用安装向导

1. 完全退出 PowerPoint。
2. 双击 `安装中心.exe`；如果无法启动，双击 `安装中心.bat`。
3. 在环境检查页确认 PowerPoint、发布产物和构建环境状态。
4. 选择安装方式并等待安装、自检完成。
5. 重新打开 PowerPoint，在「开始」选项卡点击「模板助手」。

安装完成页可以打开 PowerPoint、打开安装目录、启动 MCP，或创建「模板助手安装向导」桌面快捷方式。

## 两种安装方式

### 全新构建并安装

适合开发机或发布前构建。需要 Node.js 24+ 和 npm。首次从 GitHub 克隆时，脚本会在缺少依赖的情况下自动按 lockfile 执行 `npm ci`，然后完成版本生成、测试、前端构建、后端打包、安装和运行时自检。

也可以直接运行：

```powershell
.\安装.bat
```

### 使用现有发布产物

适合普通使用电脑或重复安装，不需要 Node.js。项目目录必须包含：

- `dist-exe\ppt-ai-addin.exe`
- `addin\dist`
- `manifest.xml`
- `release.json`

在安装向导选择「使用现有构建产物」，或运行：

```powershell
.\安装.bat --skip-build
```

程序安装到 `%LOCALAPPDATA%\PPT-AI-Addin`，只注册到当前 Windows 用户。

## 升级与重装

直接重新运行安装向导即可。安装过程会停止旧服务、复制新产物、重新注册加载项并核对前后端版本。完成后必须完全退出并重新打开 PowerPoint。

如果任务窗格提示版本不一致，重新运行安装向导；仍不一致时，在「AI 配置 → 关于 / 系统诊断」确认运行版本、端口和服务状态。

## 首次使用

- 模板保存和变量填充无需配置第三方 Key。
- 需要生成文字时，在「AI 配置」填写 OpenAI 兼容文本服务的地址、模型和 API Key。
- 默认图片搜索可直接使用；也可以在「图源管理」增加自定义来源。
- 界面语言在「AI 配置 → 界面显示 → 语言」切换，支持中文和 English；保存后下次打开仍会沿用。
- MCP 需要 Node.js 24+。客户端命令为 `node`，参数为项目绝对路径下的 `server\mcp\index.js`。工具与安全机制见[技术文档](docs/插件技术总结.md)。

## 数据迁移

模板和套版默认位于 `%USERPROFILE%\Documents\PPT模板库`。迁移时复制整个目录到新电脑的同一位置。

配置和运行信息位于 `%APPDATA%\ppt-ai-addin`。API Key 使用当前 Windows 用户的 DPAPI 加密，不应直接复制到另一个账户；请在新电脑重新填写。

下载图片默认保存在 `%USERPROFILE%\Documents\PPT下载图库`。

## 卸载

双击 `卸载.bat`，或从安装向导欢迎页进入卸载流程。卸载会停止本地服务、移除登录自启动和 PowerPoint 加载项注册，并询问是否删除安装目录。

模板库不会默认删除；如不再需要，请自行备份后删除 `%USERPROFILE%\Documents\PPT模板库`。

## 常见问题

### PowerPoint 中没有「模板助手」

确认安装自检成功，完全退出所有 PowerPoint 进程后重新打开。仍未出现时重启 Windows，再运行一次安装向导。

### 本地服务未启动

在「AI 配置 → 关于 / 系统诊断」查看状态，或运行：

```powershell
.\scripts\verify-install.ps1
```

服务默认使用 `127.0.0.1:3788`；端口被占用时会尝试后续端口，并把实际端口写入运行时文件。

### 安装脚本提示缺少发布产物

需要源码构建时安装 Node.js 24+ 并选择「全新构建并安装」。如果使用 `--skip-build`，请确认上文列出的四项发布产物完整。

### 写入当前 PowerPoint 失败

确认 PowerPoint 已打开目标演示文稿。若 PowerPoint 与本地服务以不同权限运行，COM 直接写入可能失败；打开任务窗格后可从待写队列再次写入。
